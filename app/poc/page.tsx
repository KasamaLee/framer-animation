import AnimationCompare from "../AnimationCompare";

export const metadata = {
  title: "Animation Proof of Concept — Framer Motion vs GSAP",
  description: "Detailed live performance comparison between Framer Motion and GSAP for card stacking, parallax, and scroll smoothing.",
};

export default function PocPage() {
  return <AnimationCompare />;
}
